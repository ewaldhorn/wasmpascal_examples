# Hello Write

Demonstrates standard output streaming using `write` and `writeln` in [WasmPascal](https://wasmpascal.com/).

Exercises text output for string literals, integers, floating-point numbers, and explicit newline control through the WebAssembly host environment.
