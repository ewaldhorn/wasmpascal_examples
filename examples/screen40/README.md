# 40x10 Screen Layout

Demonstrates custom text-screen dimensions using the `{$Screen 40 10}` directive in [WasmPascal](https://wasmpascal.com/).

Configures the virtual console to a compact 40-column by 10-row character grid, verifying cursor placement, borders, and text wrapping within custom screen geometries.
