# Variable (Var) Parameters

Demonstrates pass-by-reference semantics using `var` parameters in [WasmPascal](https://wasmpascal.com/).

Shows how passing variables by address enables in-place mutation of caller variables, record fields, and array elements across function calls.
