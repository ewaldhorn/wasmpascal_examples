# Classic Dots

This is a typical classic, DOS-era screensaver, updated a bit to run on the web, of course. The demo is written in Pascal and makes use of the HTML Canvas integration built into [WasmPascal](https://wasmpascal.com/). This code is specific to the WasmPascal compiler in that it uses extensions to the core Pascal language to allow Pascal to control web graphics.
