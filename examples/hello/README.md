# Hello World Library

A minimal Pascal library compiled to WebAssembly with [WasmPascal](https://wasmpascal.com/).

Demonstrates the Pascal `library` and `exports` syntax, exporting native functions like `add` and `wasm_init` to the JavaScript host alongside printing to the standard output.
