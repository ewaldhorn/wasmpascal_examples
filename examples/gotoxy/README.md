# GotoXY and ClrScr

Demonstrates terminal cursor positioning using `GotoXY(x, y)` and screen clearing with `ClrScr` in [WasmPascal](https://wasmpascal.com/).

Shows how Pascal character-cell coordinates map directly to the virtual 80x25 terminal grid to draw borders, banners, and centered messages.
