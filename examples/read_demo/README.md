# Readln Demo

Demonstrates interactive console input using standard Pascal `readln` in [WasmPascal](https://wasmpascal.com/).

Input prompts for integers and floating-point values are routed through the Web Worker runtime to browser dialog prompts, reading values back into Pascal variables seamlessly.
