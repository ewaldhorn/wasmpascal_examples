# Colors Demo

Demonstrates [WasmPascal](https://wasmpascal.com/)'s ability to drive both text console output and an HTML5 graphics canvas side by side.

This example renders smooth color gradients onto the 2D canvas while simultaneously printing colored text using `TextColor` and `TextBackground` to the output terminal, illustrating the dual output capabilities of the compiler.
