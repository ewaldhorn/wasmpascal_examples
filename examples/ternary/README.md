# Ternary Expressions

Demonstrates modern Delphi-style ternary conditional expressions in [WasmPascal](https://wasmpascal.com/).

Shows how expressions of the form `if condition then expr1 else expr2` can be used directly inside assignments and function arguments, supporting numeric type promotions and string types.
