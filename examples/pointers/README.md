# Pointer Ergonomics

Demonstrates pointer syntax, type safety, and memory ergonomics in [WasmPascal](https://wasmpascal.com/).

Covers:
- Typed pointer declarations (`^TNode`).
- The `nil` null-pointer constant.
- Inspecting type sizes with `SizeOf`.
- Pointer arithmetic using `Inc` and `Dec` across array elements.
