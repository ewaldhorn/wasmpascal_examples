# Fibonacci

A classic recursion and iteration benchmark demonstrating both recursive and looping Fibonacci calculations in [WasmPascal](https://wasmpascal.com/).

Also highlights Delphi-style inline ternary expressions (`if cond then a else b`) used directly inside expression contexts.
