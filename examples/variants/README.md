# Variant Records

Demonstrates Pascal variant records (`case tag of`) in [WasmPascal](https://wasmpascal.com/).

Shows how overlapping field layouts allow memory-efficient tagged unions (such as geometric shape definitions) compiled into WebAssembly memory structures.
