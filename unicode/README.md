# Unicode and String Literals

Demonstrates Unicode and extended character handling in [WasmPascal](https://wasmpascal.com/).

Covers:
- Raw UTF-8 literals preserved through compilation.
- Decimal character codes (`#nn`) and hexadecimal codes (`#$hh`).
- Adjacent string literal concatenation matching classic Turbo Pascal rules.
