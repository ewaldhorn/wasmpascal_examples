# With Statement Demo

Demonstrates Pascal's `with` statement in [WasmPascal](https://wasmpascal.com/).

Shows how opening record member scopes simplifies field access, supports in-place field updates, and resolves shadowed fields in chained `with a, b do` statements.
